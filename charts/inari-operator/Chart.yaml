apiVersion: v2
name: inari-operator
description: >-
  Inari platform operator — reconciles platform.inari.io resources (Keycloak
  realms/clients, DNS zones/records, cert issuers, ArgoCD projects, tenant
  namespaces).
type: application
version: 0.1.2
appVersion: 0.0.8
