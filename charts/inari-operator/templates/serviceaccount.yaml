apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "inari-operator.serviceAccountName" . }}
  labels:
    {{- include "inari-operator.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
